name: pull and push request

on:
  push:
  branches:
  -main
  pull_request:
  branches:
  -main

jobs:
  my-job:
    runs-on: macOS-latest

    steps:
    - name: set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Unit test
      run: ./gradlew test
